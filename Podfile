workspace 'MEMBase'
project 'Example/MEMBase.xcodeproj'
use_frameworks!

platform :ios, '15.0'

target 'MEMBaseExample' do
  pod 'MEMBase', :path => './'
  pod 'Kingfisher', '~> 7.0'
  
  target 'MEMBaseTests' do
    inherit! :search_paths

    
  end
end
